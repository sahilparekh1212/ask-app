# PROD — AUTH_RSA_PRIVATE_KEY and Google credentials MUST be supplied via secret.
# A stable RSA key is mandatory so horizontally-scaled replicas validate each other's tokens.
logging.level.root=WARN
logging.level.com.askapp=INFO
