# TEMPLATE — copy to secret.yaml (gitignored) and set a real password, or create it
# imperatively:
#   oc create secret generic grafana-secret -n ai-sandbox \
#     --from-literal=admin-user=admin \
#     --from-literal=admin-password='<strong-password>'
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: ai-sandbox
type: Opaque
stringData:
  admin-user: admin
  admin-password: change-me
