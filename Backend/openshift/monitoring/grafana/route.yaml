apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: grafana
  namespace: ai-sandbox
  labels:
    app: grafana
spec:
  to:
    kind: Service
    name: grafana
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
