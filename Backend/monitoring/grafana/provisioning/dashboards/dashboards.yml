# Loads dashboard JSON files bundled under /var/lib/grafana/dashboards.
apiVersion: 1

providers:
  - name: askapp
    orgId: 1
    folder: ask-app
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
