{
  "name": "ask-app-e2e",
  "version": "0.0.1",
  "private": true,
  "description": "Playwright end-to-end suite that drives the full docker-compose stack: browser -> nginx -> Auth -> Kafka -> Audit -> Postgres.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
